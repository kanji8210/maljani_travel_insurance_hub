# ── React / Vite frontend (not part of the deployed WP plugin) ──────────────
frontend/

# ── tick/ React app dependencies (install locally via npm install) ───────────
tick/node_modules/
tick/node_modules - Copie/

# ── OS / Editor ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
*.swp
*~

# ── PHP composer vendor ──────────────────────────────────────────────────────
vendor/

# ── Node (if any tooling is added at plugin root) ────────────────────────────
node_modules/
.env
 

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json
